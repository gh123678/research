# 组会汇报：softmax 注意力能否实现"可证明的"策略改进

> 汇报人自用稿。全部数字可在仓库 `results/` 中复核；标注【预测】的条目尚未完成验证。
>
> **状态说明（2026-09-12 追加，非原作者所写）**：本稿是当日 20:00 的快照，正文未作改动。
> 稿中"阶段 6 / 下一步"当时仍在进行的两项现已完成，结果见文末 **九、会后更新**；
> 引用第五步表格时请连同第六步一起看。

---

## 一、研究背景与目标（2 分钟）

### 出发点：一篇 ICRL 论文

《Beyond Linear Attention: Softmax Transformers Implement In-Context RL》（arXiv 2026）证明了：

- **Theorem 1**：softmax 注意力（双头，后简化为单头 + TSM 模块）的前向传播，精确等价于一种新的 TD 算法（weighted softmax TD）的一次迭代；
- **Theorem 2**：在"核对角占优"假设下（softmax 对自身状态的权重足够大），层数和上下文同时增大时收敛到真实价值函数 v^π；
- 这些参数是预训练损失的全局极小，解释了实验中结构的出现。

**但这篇论文只做了一件事：策略评估（policy evaluation）。** 全文 "policy improvement" 出现 0 次，"greedy / policy iteration / Q-learning" 各 0 次。

### 我们的问题

> **同一套固定权重的 softmax 注意力架构，能不能不只是"评估一个策略"，还能"把策略改好"——并且这个"改好"要有有限样本的误差保证，而不是只在实验里看着变好？**

这是对论文的自然延伸：评估是 RL 的第一步，改进是第二步。论文停在了第一步。

### 为什么强调"可证明"

ICRL 的场景是部署后参数冻结、靠前向传播自适应。这种场景下"经验上变好"不够——你需要知道这次更新**会不会让某些状态变差**。所以我们的目标不是"让回报变高"，而是：

> 每一次策略更新，都要附带一个**可证伪的证书**：要么证明"逐状态不退化"，要么明确弃权。

---

## 二、核心思路（3 分钟）

整个方法由三层组成，每层都可以单独检验：

### 第 1 层：改进算子 —— 相对 softmax 倾斜

```
π⁺(a|s) ∝ π(a|s) · exp(η · Q̂(s,a))
```

往 Q̂ 高的动作挪一点概率。由一个 KL 恒等式立刻得到：

> **只要 Q̂ 在某状态不是常数，任意 η > 0 都给出严格正的改进。**（定理层面，"能改进"是白送的）

### 第 2 层：有限样本证书 —— E_Q

Q̂ 是估的，不是真的。证书给出：

```
|| Q̂ − Q^π ||∞ ≤ E_Q     （以概率 1−δ 成立）
```

构造：留出数据分两半，一半估残差尺度，一半估残差均值，各用一次 Hoeffding，union bound 合并。

### 第 3 层：决策规则 —— 发出或弃权

对每个状态算：

```
LB_s = 估计的改进信号 Î_s  −  E_Q × 策略改动幅度
```

**所有状态的 LB_s > 0 才发出更新；否则原样返回策略并记录原因。** 由此得到定理：

> P（发出了更新 且 存在状态变差）≤ δ

### 关键的方法学选择：固定策略

每一步迭代时策略是固定的。原因：**固定策略下贝尔曼残差无偏（E[Y]=0），证书是纯集中不等式问题**。实测：残差均值中位只有 E_Q 的 1.9%，非常干净。一旦策略随迭代变化，无偏性消失，分析难度上一个台阶——所以先把固定策略这一步做透。

---

## 三、研究历程与困难（5 分钟）

这条线不是一帆风顺的，困难本身就是结果的一部分。

### 阶段 1：连续两次"全军覆没"（负结果）

| 任务 | 证书发出 | 更新发出 |
|---|---:|---:|
| FP-ADV-001 | — | **0 / 480** |
| FP-ESARSA-001 | 283 / 480 | **0 / 480** |

证书能算出来，但决策规则**一次都不敢放行**。这是最困难的时期：系统没有任何 bug，全部检查通过，就是发不出更新。

### 阶段 2：诊断 —— 把"发不出"变成一个数（FP-SCALE-001）

逐项测量后定位：

1. 瓶颈不在经验残差（实测 2.41）而在集中半径（37.76），**差一个数量级**；
2. 发出更新的必要条件是 **E_Q < σ**（状态内动作价值的落差）——因为改进信号是二阶的、误差项是一阶的；
3. E_Q 和 σ 都随奖励幅度线性放大，所以"把数值谱拉宽"没用（实测：谱宽 6 倍，E_Q 涨 170 倍）;
4. **病灶是最坏情况包络 2B**：证书假设残差 |Y| 可以达到理论上界 10。

**这一阶段没有产生任何新算法，但它是整个项目最重要的转折：把"为什么不行"变成了"哪个量在挡路"。**

### 阶段 3：修复 —— 方差自适应证书（FP-SCALE-002）

只换一个零件：把包络 2B 换成"用留出数据自己估尺度"，常数全部来自命名不等式。结果：

| 指标 | 结果 |
|---|---|
| 更新发出 | **22 / 48（45.8%）** |
| 证书违规 | **0** |
| 发出的更新 | 逐状态不退化 + 总价值严格上升（22/22） |
| E_Q | 均值 0.2426，对照组 1.0744（**紧 3.4–5.5 倍**） |

**项目第一个正结果。**

### 阶段 4：落地 —— 从公式回到网络（FP-ATTN 系列）

此前所有结果都是 numpy 公式算的。但项目的论点是关于**注意力网络**的。于是逐层对齐：

- FP-ATTN-001：字面注意力网络复现第一步，与 numpy 一致；
- FP-ATTN-ITER-001 / ITER4：网络跑 3 步、4 步迭代，决策、η、E_Q 与 numpy 完全相同；
- 网络（float32）vs numpy（float64）的偏差约 **1e-5，且不随步数累积**。

### 阶段 5：迭代 —— 能走多远（FP-ITER2–6）

| 步数 | 发出的记录数 | 平均总价值增量 |
|---:|---:|---:|
| 1 | 22 | 2.72 |
| 2 | 20 | 2.28 |
| 3 | 15 | 1.29 |
| 4 | 12 | 0.81 |
| 5 | **12** | 0.36 |

- 第 4、5 步发出的是**同一批 12 条记录**（平台期）；
- 48 条记录自然分成三组：**26 条从未发出、10 条中途掉队、12 条走满五步**；
- "第 5 步继续减少"的预测**被证伪**——过程进入了稳定 regime 而不是走向终止。

### 阶段 6：归因 —— 先写预测，再做实验（FP-CENSUS-001，进行中）

正在检验：每条的 **σ_min / E_Q**（状态内价值落差 ÷ 证书误差）能否**预测**谁能走到第 6 步。预测已带时间戳冻结，再跑实验评分。

> 这是从"描述过去"到"预测未来"的一步——也是防"事后编故事"的设计。

---

## 四、主要科学发现（4 分钟）

### 发现 1：瓶颈不在改进机制，在证书

用真实 Q 审计：**192 个状态的真实改进全部为正（192/192）**。弃权的 26 条记录里，每一个状态其实都在改进（中位 +0.052），只是证书证不出来（下界中位只有 +0.009）。

> **程序不是在拒绝坏的更新，而是在拒绝它证不出来的好更新。**

### 发现 2：证书误差的 84% 不是数据，是假设

对全部 576 个格子逐格分解 E_Q 的构成：

| 来源 | 占 s² 的比例 |
|---|---:|
| 数据（实测残差尺度） | **16.3%** |
| 假设（"\|Y\| 可能到 10"的包络项） | **83.7%** |

而数据里 |Y| 最大只有 **2.25**。在 564/576 个格子里，假设项大于数据项。

> **"我们对 Q 的不确定度"有八成来自一个可以被数据检验、且被数据推翻 4 倍的最坏情况假设。**

### 发现 3：可证明改进的判据是一个比值

发出 ≈ 要求 **E_Q < 状态内价值落差**（精确形式与状态内 Q 的形状有关）。实测：发出记录 E_Q 中位 0.2107，弃权记录 0.2255——几乎一样，**能不能发出基本由 E_Q 决定，而不是记录好坏**。

### 发现 4：迭代的衰减是结构性的

迭代中实测：

- E_Q 五步基本不变（≈0.22）；真实误差也不变（≈0.02）；
- **每步可证余量持续衰减**：0.0120 → 0.0076 → 0.0029 → 0.0009。

> "走不远"不是证书变松，而是**改进信号在缩、证书大小不变**——这个错配是结构性的，解释了平台期的出现。

### 发现 5：与论文的结构呼应

论文的收敛性证明依赖"核对角占优"（softmax 要足够认出自己）。**我们的迭代边界，恰好落在同一个结构上**——它是论文假设在"策略改进"一侧的实测边界。同一数学对象的两面。

### 负结果（同样写入记录）

- 跨状态核泛化：两条路线均 **NOT_SUPPORTED**；
- 记录复用/借用：**NO_BORROWING_EVIDENCE**；
- 三个早期任务：**0/480**（已验证的负结果，非失败）。

---

## 五、方法上的治理（可一句带过）

- 每个正式任务先写**可证伪假设 + 冻结协议 + 验收标准 + 停止条件**，再动手；
- 结果带版本、种子、命令、哈希封存，可复现；
- 负结果、被证伪的预期**写入记录而不是丢弃**（例：第 5 步"继续减少"的预测被证伪后如实报告）；
- 目前大部分任务为**单执行者执行 + 同执行者导出验证**（验证重算每个数、回放封存程序，但无第二执行者独立重建）——**这是当前验证强度的上限，在所有报告中如实声明**。

---

## 六、局限（主动讲，2 分钟）

1. **玩具规模**：4 状态、3 动作、两个混合比。所有数字都是这个规模下的；"84% 来自假设"这类结论的**规模依赖性未知**（状态变多后最坏 pair 会更极端，占比可能更高）。
2. **单执行者验证**：numpy 与网络两条路径一致，只能排除实现错误，**不能排除共同的概念错误**。
3. **迭代部分只有实证、没有定理**：收敛到最优既未定义也未尝试；迭代后无偏性消失，理论分析是开放问题。
4. **部分数字是反事实估计**【预测】：例如"常数收紧后 22/48 → 30–35/48"是用现有记录乘系数估的，新证书尚未实现和验证。

---

## 七、下一步（按优先级）

1. **收紧证书**【预测：E_Q 0.22 → 0.08–0.12，发出 22/48 → 35+/48】
   - 常数写紧（同一 Hoeffding 的等价紧形式，约 1.8 倍）；
   - 让每个格子用自己的观测尺度替代全局包络（去掉了 84% 的假设项）。
   - 注意：这是新任务，不改任何已冻结的协议。
2. **完成 CENSUS 预测评分**：σ_min/E_Q 判据成立 → 得到资格预测器；不成立 → 约束在别处（最坏 pair 或核对角），同样是结论。
3. **画"样本量 vs 可证改进"曲线**：回答"一步改进值多少样本"——这是把单步结果推向迭代理论前必须先有的数。

---

## 八、一句话总结

> **softmax 注意力网络不仅能在前向传播中做策略评估（论文的结果），还能执行带有限样本误差证书的策略改进：48 条路线中 22 条完成第一步认证改进、五步后 12 条仍在改善，两条独立计算路径一致；而改进的边界由证书的保守性决定，其 84% 来自一个可以被数据检验并推翻的最坏情况假设。**

---

## 附：可能被问的问题与回答要点

**Q1：环境只有 4 状态 3 动作，结论有什么用？**
这是可控的测试台，目的是把"证书"这个概念钉死。结论的形式（E_Q 与价值落差的关系、包络项的占比）是规模无关的命题；数值本身是规模相关的，已写入 limitation。

**Q2：192/192 真实改进都是正的，那为什么只发 22 条？**
发出需要的是"能**证明**改进"，不是"改进存在"。证书给的下界平均只有真实改进的 41%，弃权记录里只有 22%——差的那部分被 E_Q 吃掉了。

**Q3：为什么不用更大的步长直接贪婪改进？**
那就不是 softmax 的问题了。项目的科学问题恰恰是"softmax 这种平滑、局部的更新能不能带证书工作"。贪婪更新是一阶的、好证，但不在问题范围内。

**Q4：和 safe RL / offline RL 的关系？**
目标不同。safe RL 约束的是探索过程；我们约束的是**更新决策本身**——每次更新附带一个逐状态不退化的可证伪证书，发不出来就弃权。

**Q5：验证都是同一个执行者做的，可信吗？**
这是当前最大的限制，已在所有报告中声明。验证是导出式的：从封存记录独立重算每个指标、回放封存程序、核对哈希。补救方式：第二执行者独立重建（需要资源），或人工复算关键数字（已部分完成：48 条记录的 Q̂、残差、半径、E_Q 均可从源码逐位重建）。

**Q6：接下来最大的风险是什么？**
收紧证书后如果发出率仍上不去，说明"信号二阶、误差一阶"的诊断不完整——但那本身就是一个更有价值的发现（已有备选假设：最坏 pair 结构、核写回的对角占优）。

---

## 九、会后更新（2026-09-12 追加，非原作者所写）

草稿写成时列为"进行中"的两项已经完成，一并补在这里，正文其余部分保持原样。

### 第六步：平台期结束了

| 步数 | 发出的记录数 | 平均总价值增量 | 最小总价值增量 |
| ---: | ---: | ---: | ---: |
| 1 | 22 | 2.717707 | 0.104197 |
| 2 | 20 | 2.277997 | 0.779902 |
| 3 | 15 | 1.286906 | 0.378197 |
| 4 | 12 | 0.807500 | 0.184902 |
| 5 | 12 | 0.361474 | 0.082395 |
| **6** | **9** | **0.167057** | **0.019347** |

- 第四、五步"同样的 12 条"只是**一个区间**，不是不动点：第六步降到 9 条，失去 3 条、没有新增。
- 最小增益 `0.019347` 跌破了第四、五步都通过的非平凡下限 `0.05`——这一条预先登记的预测被**否定**。
- **网络与 numpy 两条路线都跑到六步，且第六步发出的是同一组 9 条记录**；129 条逐步记录里更新判断与 η 选择分歧均为 0，估计差六步内始终在 `1e-4` 容差内、不随步数增长。
- 所以第八节"一句话总结"里的"五步后 12 条仍在改善"现在应读作"六步后 9 条"。

### 资格普查：判据能描述过去，预测不了未来

对 48 条路线记录、每一步（含弃权步）测 `σ_min / E_Q`：

- 第一，**普查里每一次弃权的原因都是同一条** `improvement_lcb_nonpositive`——证书从未失败，失败的是改进预算。这支持正文"瓶颈在证书而不在改进机制"的说法，但把它精确化为"证书不是算不出来，是证明不了"。
- 第二，**区分主要来自 σ 而不是 E_Q**：两组 σ 相差 `2.6` 倍，E_Q 只差 `1.2` 倍；单用 E_Q 预测弃权会误分类 `16/48`。这与第三节"发出 ≈ 要求 E_Q < 价值落差"的方向一致，但说明起作用的那个量是落差。
- 第三，**作为预测器它失败了**。第一步最优阈值仍有 `1/48` 误分类；更关键的是第六步停下的 3 条里有 2 条的第五步比值**高于**仍在继续的最低记录，任何阈值都分不开。原因是 σ_min/E_Q 沿轨迹几乎不变（变异系数 `0.018`），它更像记录的属性而不是状态的读数。

> 这一条对第七节"下一步"的第 2 项是个明确答复：判据成立的方向是"资格"而不是"持续时间"。原先登记的第六步预测比较因两条规则给出完全相同的预测而**退化**，这一点已作为设计缺陷写入记录，没有事后补救。

出处：`docs/research_branches/FP-CENSUS-001/claude/first_result.md`、
`docs/research_branches/FP-ATTN-ITER6-001/claude/first_result.md`。

### 仍然成立的两条限制

正文第六节的局限没有变化，另外补一条：**验证强度仍是同执行者自检**，六步的两条路线一致只能排除实现分歧，不能排除共有的概念错误。独立验证依然缺位，是全项目当前最大的未决项。
