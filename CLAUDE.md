# Claude Code repository entry point

Before taking any action in this repository, read in order:

1. `AGENTS.md`, the canonical governance policy;
2. `icrl_softmax/ACTIVE_WORKSPACE.md`, the current scientific and task state;
3. the active task record linked from that file.

Remain read-only unless the active task explicitly grants Claude an execution scope. When execution is granted, work only on the named `claude/*` branch, in the named files and output directory.

Do not edit a GPT-authored task definition, acceptance criterion, GPT branch, or another route's results. If a task-level defect or conflict is found, return `OBJECTION` with the disputed clause, evidence, impact, and options, then stop the affected work for the user's ruling.

Do not duplicate or reinterpret the full policy here. Direct user instructions have highest authority; otherwise `AGENTS.md` controls.
