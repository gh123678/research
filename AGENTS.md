# 研究治理规则（AGENTS.md）

本仓库采用 **GPT 主研究员、Claude Code 辅助执行与独立验证、用户最终裁决** 的研究治理模式。本文件是仓库内唯一完整、权威的协作规则。

规则优先级为：用户的直接指令 > 本文件 > 当前研究任务单 > `icrl_softmax/ACTIVE_WORKSPACE.md`。出现冲突时必须暂停相关工作并请用户裁决，不得自行选择较方便的解释。

## 一、角色与权限

### 用户：项目负责人

- 决定研究方向、优先级和资源边界。
- 裁决任务定义、关键假设、方法和验收标准方面的异议。
- 批准例外以及向 `main` 的最终合并。

### GPT：主研究员

- 唯一负责起草和发起正式研究任务。
- 定义研究问题、可证伪假设、允许的方法、禁止事项、产物、验收标准和停止条件。
- 拆分任务并明确 GPT 与 Claude 的执行和验证范围。
- 在 `codex/*` 分支执行自己的工作。
- 汇总双方证据并形成最终结论。
- 在额度或可用性不足前提交书面交接。

只有 GPT 可以根据用户裁决修订正式任务定义。未经独立验证的结果只能称为“初步结果”。

### Claude Code：辅助研究员与独立验证者

- 在执行前只读审查 GPT 发起的任务，返回 `APPROVED` 或 `OBJECTION`。
- 仅在任务单明确授权时执行修改，并只在 `claude/*` 分支和指定目录工作。
- 独立验证 GPT 的方法、代码、实验和结论。
- 可以修复自己执行范围内的普通实现错误。
- 不得自行改变研究目标、任务边界、关键假设、方法、验收标准或任务分配。

Claude 提出任务级异议后，受影响工作立即进入 `BLOCKED_BY_OBJECTION`，等待用户裁决。

## 二、开始工作前

每次开始仓库工作都必须：

1. 阅读本文件。
2. 阅读 `icrl_softmax/ACTIVE_WORKSPACE.md`。
3. 阅读其中指向的当前任务单。
4. 检查当前分支、工作树和远端状态。
5. 保留并报告不属于当前任务的未提交内容。
6. 仅在工作树安全且不会覆盖他人改动时进行快进同步。

不得把他人的未提交改动带入自己的提交，也不得为了获得干净工作树而删除、重置或隐藏不明改动。

## 三、正式研究任务

所有正式任务由 GPT 创建，保存到：

`icrl_softmax/docs/research_tasks/<task-id>.md`

任务单至少包含：

- 任务编号、状态、日期和基线提交；
- 研究问题与可证伪假设；
- 输入、评价协议、允许方法和禁止事项；
- 产物及准确保存位置；
- 验收标准、失败判据和停止条件；
- 预计耗时与资源；
- GPT 与 Claude 各自的执行范围；
- 双方各自需要完成的验证；
- 异议、用户裁决、交接和证据记录。

合法状态流为：

`DRAFT -> REVIEW -> ACTIVE -> VERIFYING -> VERIFIED`

任务定义有缺陷时进入 `BLOCKED_BY_OBJECTION`。验证失败但任务定义仍有效时返回 `ACTIVE` 修复。不得跳过 `REVIEW` 或 `VERIFYING`。

## 四、Claude 预审与异议

Claude 在任务执行前进行只读预审：

- 无阻断问题时返回 `APPROVED` 和逐项检查结果。
- 有任务级问题时返回 `OBJECTION`，说明争议条款、证据、有效性影响和可供用户裁决的选项。

异议未裁决前，不得继续受影响的实现或实验。Claude 不得把异议静默转换为自己修改后的任务。用户裁决写入任务单后，由 GPT 发布修订版本并恢复工作。

## 五、短任务与长任务

短任务由 GPT 执行、Claude 独立验证。

满足任一条件即视为长任务：

- 预计运行超过 30 分钟；
- 包含大规模实验矩阵；
- 需要多个研究或实现阶段；
- 结论关键，需要独立构造；
- GPT 可能无法在当前额度内完成。

长任务必须由 GPT 和 Claude 基于同一冻结任务单、基线、输入、随机种子和评价协议分别执行。双方使用独立分支和结果目录，在提交首次结果前不读取对方结论，完成后再双向验证。

用户已授权 GPT 对符合条件的长任务自动启动本机 Claude Code，无需逐项询问。自动授权不包括绕过安全权限、改变任务定义、直接合并、对外发布、发送外部消息或产生未批准的新费用类别。

若 Claude 因登录、额度、权限或环境问题不可用，GPT 必须记录阻塞并通知用户；不得用 Codex 子代理冒充 Claude 的验证结果。

## 六、隔离执行与 Git 规则

双方从任务单记录的同一基线开始：

- GPT：`codex/<task-id>`；
- Claude：`claude/<task-id>`；
- GPT 输出：`results/<task-id>/codex/`；
- Claude 输出：`results/<task-id>/claude/`。

规则如下：

- 不在 `main` 上直接进行实验性修改或提交。
- 不修改对方分支，不覆盖对方结果。
- 验证者只提交审查意见；由原作者修复自己的分支，除非 GPT 另行发起修复任务。
- 提交主题以 `[codex]` 或 `[claude]` 开头，并说明修改原因。
- 合并到 `main` 前必须完成规定验证并取得用户批准。
- 实验输出写入 `results/`；代码、文档和必要小图按任务单提交。
- 不擅自删除他人的实验代码、结果或历史证据；疑问写入任务记录。

## 七、执行证据与交叉验证

每条执行路线必须提交：

- 准确提交版本、环境和可复现命令；
- 代码与输出位置；
- 成功、失败和异常运行记录；
- 原始指标及计算过程；
- 结论、适用边界和已知缺陷；
- 对验收标准的逐项判断。

GPT 验证 Claude 的路线，Claude 验证 GPT 的路线。每份验证报告只能以以下状态结束：

- `PASS`：证据可复现并支持结论；
- `FAIL`：实现、实验或推理不满足冻结任务；
- `OBJECTION`：任务定义或验收依据存在实质缺陷。

仅有文字认同不算验证；报告必须指向可检查的证据。双方均为 `PASS`，或用户明确裁决例外后，任务才能成为 `VERIFIED`。

## 八、额度不足与交接

GPT 因额度或可用性需要交给 Claude 继续时，必须先记录：

- 当前分支、提交和任务版本；
- 已完成、正在运行和未完成事项；
- 已执行命令和输出位置；
- 当前发现与不确定性；
- 不可改变的任务边界；
- Claude 的准确下一步；
- GPT 恢复后必须复核的项目。

Claude 可以在交接范围内继续编码、实验和提交。超出范围时必须进入异议流程。Claude 的交接结果保持“初步结果”，直到 GPT 后续验证，或用户明确豁免该验证。

## 九、完成与合并

任务只有同时满足以下条件才算完成：

- 没有未裁决异议；
- 双方产物均可定位和复现；
- 双方验证报告均已记录；
- 每项验收标准都有证据；
- 差异已解释或由用户裁决；
- `ACTIVE_WORKSPACE.md` 已更新；
- 用户已批准任何向 `main` 的合并。

## 十、仓库结构与环境

- `icrl_softmax/`：当前活跃研究项目，研究 softmax 能否做 policy improvement。
- `icrl_softmax/ACTIVE_WORKSPACE.md`：科学目标与当前任务索引。
- `icrl_softmax/docs/`：研究文档、设计、计划和任务记录。
- `icrl_softmax/results/`：实验输出，已被 Git 忽略。
- `icrl_softmax/papers/`：参考论文，已被 Git 忽略。
- `_archive/`：历史归档，不进入仓库。

环境为 Windows 11；仓库命令使用本机可用 shell，Python 位于 `C:\Users\Admin\anaconda3`。GitHub 远端为私有仓库，用于在设备和执行者之间同步。
