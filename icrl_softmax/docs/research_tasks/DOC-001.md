# DOC-001: Refresh README to the current fixed-policy research state

## Task metadata

- Created: 2026-09-01
- Author: GPT
- Status: `VERIFYING`
- Task version: `1.0`
- Baseline commit: `4c89263`
- Design: `docs/superpowers/specs/2026-09-01-readme-current-state-refresh-design.md`
- Plan: `docs/superpowers/plans/2026-09-01-readme-current-state-refresh-plan.md`
- GPT branch: `codex/DOC-001`
- Claude role: read-only pre-review and final independent verifier
- Estimated runtime and resources: under 30 minutes of document work; four lightweight local verifiers; no formal experiment rerun

## Research question

Can the project README be made to accurately represent the completed fixed-policy Direct-Q/V-first finite-sample stage, retain the earlier sampled-SARSA construction as clearly marked history, and render reliably on Windows and GitHub without changing scientific claims or code?

## Falsifiable hypotheses

1. Every current-state claim in the rewritten README is supported by `ACTIVE_WORKSPACE.md`, the shared finite-sample theorem, or the Direct-Q/V-first report.
2. Every relative link and every listed Python entry point resolves in the active checkout.
3. The README is valid UTF-8 without replacement characters, suspicious controls, BOM dependence, or Unicode tree glyphs.
4. The three fixed-policy contract verifiers and the historical end-to-end sampled-SARSA verifier all pass unchanged.

Any failed claim-to-source check, missing path, malformed encoding check, or failed verifier rejects the corresponding hypothesis.

## Inputs and fixed protocol

Authoritative inputs:

- `icrl_softmax/ACTIVE_WORKSPACE.md`;
- `icrl_softmax/docs/research_branches/shared_fixed_policy_finite_sample_theory.md`;
- `icrl_softmax/docs/research_branches/direct_q_vs_v_first_report.md`;
- current CLI definitions in the listed evaluator, analyzer, and verifier scripts;
- `.gitignore` for the availability of results and papers in a fresh clone.

Fixed checks:

- parse all relative Markdown links and require every target to exist;
- parse every `python -B ...py` entry and require the script to exist;
- check strict UTF-8, no BOM, no `U+FFFD`, no suspicious controls, paired code fences, and no `├`, `└`, or `│` glyphs;
- run, from `icrl_softmax/`:
  - `python -B verify_finite_sample_theorems.py`;
  - `python -B verify_fixed_policy_q_routes.py`;
  - `python -B verify_crossfit_markov_certificate.py`;
  - `python -B verify_end_to_end_sarsa.py`.

The 480-item formal matrix is not rerun because this task changes documentation only. Its reported values are checked against the existing formal report.

## Allowed work

- `icrl_softmax/README.md`;
- this task record;
- the linked README design and implementation plan;
- a minimal `ACTIVE_WORKSPACE.md` pointer required by the governance definition of done;
- local read-only inspection and the four fixed verifier commands.

## Prohibited work

- changing research code, tests, experiments, results, figures, manuscripts, or archived material;
- changing any fixed-policy theorem, metric, seed, protocol, or numerical conclusion;
- rerunning or tuning the 480-item formal matrix;
- editing GOV-001 artifacts or rewriting Git history;
- merging to `main`, force-pushing, or publishing externally without user approval.

## Expected artifacts

- `icrl_softmax/README.md`;
- `icrl_softmax/docs/superpowers/specs/2026-09-01-readme-current-state-refresh-design.md`;
- `icrl_softmax/docs/superpowers/plans/2026-09-01-readme-current-state-refresh-plan.md`;
- `icrl_softmax/docs/research_tasks/DOC-001.md`;
- a minimal task pointer in `icrl_softmax/ACTIVE_WORKSPACE.md` if needed at completion.

## Acceptance criteria

1. The README leads with the current fixed-policy research question and completed 2026-08-31 status.
2. Direct-Q, V-first no-split, shared certificates, assumptions, and open problems match the authoritative documents.
3. The 480-item summary reproduces the accepted numerical claims without strengthening them.
4. The old sampled-SARSA construction remains available but is explicitly historical and bounded by its original assumptions.
5. Obsolete active commands and stale `diag_*`/`train_*` directory claims are absent.
6. All relative links and Python entry points resolve.
7. Encoding and Markdown structure checks pass without relying on a BOM or Unicode tree glyphs.
8. All four fixed verifier commands exit zero with their expected PASS marker.
9. The final diff changes only the allowed documentation paths.
10. Claude independently returns `PASS`, or the user records an explicit exception.

## Failure criteria

- Any README statement contradicts an authoritative source or overstates evidence.
- A listed command or link is missing or materially incomplete.
- A verifier fails or a formatting/encoding check finds corruption.
- The task requires a code, result, theorem, or protocol change.
- The final diff includes an unapproved path.

## Stopping conditions

Stop and notify the user if:

- Claude returns `OBJECTION`;
- concurrent work overlaps an allowed DOC-001 path;
- GOV-001 cannot be kept isolated from DOC-001;
- a scientific discrepancy cannot be resolved from frozen sources;
- Claude is unavailable because of authentication, quota, permissions, or environment failure.

## Execution evidence

### GPT route

- Commit and environment: implementation commit `b165ab6`; Windows 11; branch `codex/DOC-001`; Python 3.13.9; NumPy 2.4.6; SciPy 1.16.3; Matplotlib 3.10.6; PyTorch 2.11.0+cpu.
- Commands:
  - strict UTF-8, BOM, control-character, code-fence, tree-glyph, relative-link, Python-entry-point, and obsolete-reference scan over `README.md`;
  - `rg` claim-to-source checks against `ACTIVE_WORKSPACE.md`, the shared theorem, the route report, and `.gitignore`;
  - `python -B verify_finite_sample_theorems.py`;
  - `python -B verify_fixed_policy_q_routes.py`;
  - `python -B verify_crossfit_markov_certificate.py`;
  - `python -B verify_end_to_end_sarsa.py`;
  - `git diff --check -- icrl_softmax/README.md` and scope inspection from baseline `4c89263`.
- Outputs:
  - 22 relative links, 0 missing; 8 Python entry points, 0 missing;
  - valid UTF-8, no BOM, 0 replacement characters, 0 suspicious controls, 10 paired fence markers, 0 Unicode tree glyphs, and 0 obsolete V2/V3 or `diag_*`/`train_*` references;
  - `PASS shared fixed-policy finite-sample theorem verification`;
  - `PASS fixed-policy Direct-Q and V-first formula verification`;
  - `PASS cross-fit and Markov certificate verification`;
  - `PASS end-to-end softmax SARSA construction`.
- Claim-to-source mapping:
  - completion date and 480 same-seed comparisons: `ACTIVE_WORKSPACE.md:5,53`;
  - Direct-Q fixed-residual/no-layerwise-concentration argument: shared theorem line 10;
  - V-first no-split ghost-target argument: shared theorem line 11 and route report line 202;
  - theorem exclusions: shared theorem line 15;
  - route means, paired interval, no-uniform-dominance statement, mismatch, certificate pass rates, and long-trajectory pathwise rates: route report lines 5, 19, 22, 156, 209, 211, and 213;
  - ignored `results/` and `papers/`: `.gitignore` lines 5-6.
- Metrics and anomalies: the pre-task shared-index race was isolated by `4c89263`; the duplicate governance patch `c71392c` was removed from DOC-001's net diff by `e187997`, while the identical patch remains on governance branch commit `a096deb`; no content was lost. The Git-generated subject of `e187997` lacks the preferred `[codex]` prefix and is retained as a recorded governance-format anomaly at the user's instruction to continue research first.
- Conclusion and limitations: the README matches the frozen fixed-policy evidence and preserves sampled-SARSA as bounded historical work. The 480-item experiment was not rerun because no code or result changed.
- Acceptance assessment: criteria 1-9 `PASS`; criterion 10 pending Claude final verification.

### Claude route

- Pre-review: `APPROVED` on 2026-09-01 by Claude Code 2.1.138 in read-only plan mode.
- Pre-review scope: exactly `AGENTS.md`, this task record, the approved design, and the implementation plan; the README draft and Git diff were explicitly excluded.
- Pre-review result: no task-level defect, no objection, no permission denial, and no file modification.
- Pre-review cost: USD 0.291939, below the user-approved USD 0.75 cap.
- Non-blocking notes: independently inspect the pre-existing draft; constrain task-record edits to status/evidence fields; enumerate claim-to-source checks; record literal PASS markers for all four verifiers.
- Final verification: pending.
- Scope: read-only inspection of the frozen task and final DOC-001 diff.

## Objections and user rulings

### Objection

- Status: `NONE`
- Disputed clause: none.
- Evidence: none.
- Validity impact: none.
- Options for user ruling: not applicable.

### User ruling

- Date: 2026-09-01
- Decision: approved the recommended README design and explicitly approved isolating the work from GOV-001 into `codex/DOC-001`.
- Required GPT task revision: none.

## Quota or continuity handoff

- Current branch, commit, and task version: `codex/DOC-001`, implementation `b165ab6`, isolation `e187997`, baseline `4c89263`, task `1.0`.
- Completed, running, and pending work: design, plan, README implementation, GPT checks, and Claude pre-review are complete; Claude final verification and final evidence remain.
- Commands and outputs: recorded in GPT execution evidence above.
- Current findings and uncertainty: GPT criteria 1-9 pass; independent Claude final verification remains pending.
- Immutable boundaries: no research-code, result, theorem, protocol, or main-branch change.
- Claude's exact next actions: review the frozen task before implementation; later inspect the final diff and rerun or independently check the fixed validations.
- GPT's required return verification: inspect Claude's report, reproduce every acceptance check, and record the final state.

## Verification reports

### GPT verifies Claude

- Status: pending.
- Reproduction or inspection performed: pending.
- Evidence: pending.
- Acceptance-criteria mapping: pending.
- Required next state: pending.

### Claude verifies GPT

- Status: pending.
- Reproduction or inspection performed: pending.
- Evidence: pending.
- Acceptance-criteria mapping: pending.
- Required next state: pending.

## Definition of done

- [ ] No unresolved objection remains.
- [ ] Both required routes are reproducible.
- [ ] Both verification reports are recorded.
- [ ] Every acceptance criterion has evidence.
- [ ] Discrepancies are reconciled or ruled on by the user.
- [ ] `ACTIVE_WORKSPACE.md` is current.
- [ ] The user approved any merge into `main`.
