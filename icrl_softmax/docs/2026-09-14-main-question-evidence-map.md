# 主问题证据对照表（2026-09-14）

- 主问题：**固定权重的 softmax 注意力网络，能否把上下文经验变成可认证的、不退化的策略改进？**
- 本文只做一件事：**分清"已经证明的是哪一段、距离主问题还缺哪一段"**。不开新实验，不重述已撤回的内容。
- ~~全部条目均为**初步结果**……**没有任何一条升级为 VERIFIED**~~ → **该句在 2026-09-14 当日被 FP-COMPOSE-001 与用户裁决部分更新**：② 那一格的组合验证在**用户豁免交叉验证**的条件下记为 `VERIFIED`（附"单侧证据、无独立作者验证"的限制）。**其余所有条目仍为初步结果、仍未经任何升级**；两轮审阅（`FP-L12S-REVIEW-001`、`FP-SURVIVOR-REVIEW-001`）的**带保留判定不变**。详见下方修订记录与 §6。

> **修订记录（2026-09-14 当日追加）**：② 那一格的缺口已由 **FP-COMPOSE-001**
> 补齐并执行（192 条路线记录 / 1536 个生产者步骤，H1–H3 全部 PASS）。本文据此修订
> §1 的 ② 行、§2 与 §5，并新增 §6；**其余各行按原样保留**，未因新结果放宽任何措辞。
> 该任务的交叉验证由**用户裁决豁免**，故其结论**仅在"单侧证据 + 用户豁免"意义上成立**
> （见 `docs/research_branches/FP-COMPOSE-001/claude/user_ruling_verification_waiver.md`）。

## 1. 对照表

| 环节 | 已有支持 | 尚未支持 |
|---|---|---|
| **① 网络产生 `Q̂`** | 在**已测冻结配置**下，网络与数组实现**逐步判决一致**：原族 `FP-NET-BOUND-001`（八格、`K=16`，第一步判决 `48/48`/臂、生产者差 `1.06e-05`）；新族 `FP-NETX-002`（`6×4` 与 `4×3`，`192` 对路线记录 / `384` 个生产者-路线记录 / **`768` 对逐步判决零分歧**，frozen 专属 `Q̂` 最大差 F1 `1.156077495e-5`、F2 `1.717514838e-5`）。**独立审阅判定：frozen `768` 对逐步决策一致 PASS**，并另有审阅方**从零重跑 `32` 个生产者步骤**（`|Δ| = 0.0`）。 | **精度、种子、环境变化后的稳定性**：`float32` vs `float64` 的泛化未测；"误差不随维度增长"已被判 **FAIL** 为规律（两族同时改了奖励界、混合、任务与状态数，两点排序不能识别维度因果），现仅为"这两族上未观察到"。**`L12S` 行只能作为撤回保证后的测量记录**，不构成任何支持。**跨记录/跨臂的联合风险未分配**（当前为单轨迹级）。 |
| **② `Q̂` 到安全更新** | **`L1`/`L12` 的标量误差保证成立**（审阅 PASS；`L12` 的正确**单侧**证明见 `FP-L12-one-sided-proof.md`，算法未改）；口径：`frozen`/`L1` 对幅值目标至多花 `δ_step/2`（保守），`L12` 花满 `δ_step`，"免费"= 不额外采样、不增名义失败预算，**不是零失败概率**。**按状态更新的条件性不退化证明成立**（审阅 PASS，实数域、条件于有效 sup 范数事件）：`A_s ≥ LB_s ≥ 0`，故 `v^{π'} − v^π = (I−γP^{π'})^{-1}A ≥ 0` 逐状态成立。**接缝已补（FP-COMPOSE-001，用户豁免交叉验证）**：`producer=network × rule=perstate × 证书=L12` 已实际运行 192 条路线记录 / 1536 个生产者步骤；覆盖违规 0（最小余量 `+0.114711`）、发出步骤逐分量退化 0（最小增量 `+0.002863`）、4 个 (族,路线) 分组各 `48/48` 条网络轨迹有 ≥2 步连续发出、两生产者判决 **192/192 完全一致**（而 13,824 个 `Q̂` 中按位相等 **0** 个）。 | ~~**三项组合从未运行过**~~（**已于 2026-09-14 由 FP-COMPOSE-001 补齐**）：原状是生产者=网络只配过 `frozen` 与**已撤回**的 `L12S`，按状态规则的**长 horizon**运行只用了**数组路线**。**仍然成立、且不因新结果放宽的两条**：**"0 覆盖违规"不是有效性证据**（本项目已多次证明不成立的臂也能零违规）；**跨记录/跨臂的联合风险仍未分配**（仍是单轨迹级，`δ_total=0.05` **不覆盖** 192 条记录或两生产者）。**另加两条限制**：该组合验证**仅限 FP-COMPOSE-001 冻住的那一个协议**（两族、`K=4`、该采样与风险日程），**不是**关于该组合的定理；其证据**只有单侧作者**。 |
| **③ 更新产生实际价值** | **按状态规则缓解早停**：原族 `FP-EARLYSTOP-001` v2 —— C 胜 A `46/48`、闭合 `74.4%` vs `17.9%`，且 C 在**闭合率与数据量两个维度同时**优于四倍链臂 B（`54.9%`）。**成本档位上的排序**（新族，`FP-COST-001`）：F1 按状态 `16k`（`1.02e9` 项 / `8.6893`）高于合取 `128k`（`4.91e9` 项 / `8.6126`）；**实测档位排序 + 插值估计**，非匹配成本的实测对照。**早期推进的测量**（`FP-CLOSURE-001`）：保留的 `frozen` 臂中**第 5 步仍在发出**的记录平均闭合 **`68.8%` / `88.7%`**。 | **更一般环境下的收益与成本优势**：强命题"严格等实际总数据下显著更好"已被判 **FAIL 撤回**；逐环境并非普遍占优（F1 顶档胜 `15`/负 `33`，均值优势仅 ≈`0.89%`；F2 胜 `12`/负 `29`/平 `7`），且**无推断设计、不做显著性主张**。**后段是否仍有非零收益未被回答**——幸存者条件平均无法回答；"几何收敛/每步减半/第 10 步归零"**均不成立**。**"后段只是空转"过强**，不予采用。 |

## 2. 一句话说清"缺哪一段"

- ~~① 与 ② 各自**有条件地成立**，但**它们之间的接缝没有测过**~~ → **接缝已于 2026-09-14 补测（FP-COMPOSE-001）**：在冻住的那一个协议下，`network × perstate × L12` 跑通四步，H1–H3 全部 PASS。**但这不等于三项构成已证的系统**——见 §6。
- ③ 的**效应存在且方向明确**（按状态规则大幅缓解早停），但**成本意义上的普适优势未被支持**，且**后段收益是一个未回答的问题，不是已证的空转**。

## 3. 两条方向（用户给的框架），以及我的选择

**方向一：把现有成果做成完整论证。** 下一任务是**补齐组合验证**——冻结一个**有限主张**与**验收标准**；horizon **按问题选**，不先认定 `K≈6`。要补的具体缺口就是 §1 中 ② 那一格：`producer=network × rule=perstate × 证书=L12`，在**已测环境**上跑一次，把三段接起来。

**方向二：寻找新的科学发现。** 那要先写清：**当前生成器之外，哪一种变化可能让按状态规则失效，以及为什么值得研究**，再登记实验。（例如：状态数增大后"至少一行有 `LB_s>0`"是否仍普遍成立；行为策略的探索性下降后首访容量是否成为瓶颈；奖励界变化对 `L1` 包络的影响——`Y_RANGE=20` 已确认**不随族变化**，仅在 `R*≤3` 内够用。）

**我的建议：先走方向一。** 理由是你指出的那一条——**把分别成立的三段接成一个明确、可复核的有限结论，比现在扩维度或延长轨迹更有价值**。这与暂缓 ③ 不矛盾：③ 原本未指定它补哪个缺口，现在缺口明确了（② 那一格），再决定是否值得运行。

## 4. 若走方向一，需要冻结什么（待 GPT 起草任务单，我方可预审）

- **有限主张**：在**已测环境**（原族或 F1/F2，二选一并写明）、**已测采样协议**（首访采样、每步独立认证批、`min_visits`、链数、`δ_total`）下，`producer=network × rule=perstate × 证书=L12` 的认证迭代：每一步 **`E_Q ≥` 该步网络的实测 `‖Q̂−Q^π‖∞`**、**逐分量不退化**、并给出**实际认证数据量与墙钟**。
- **明确不主张**：不主张一般等成本优势；不主张后段收益为零或非零；不主张任何跨精度/跨种子的稳定性；不主张网络自身完成认证。
- **验收标准**：`0` 退化、`0` 覆盖违规（作为失败检测器，**不作为有效性证据**）；与**同一批数据上的数组路线**对照（配对），报告两生产者的一致性；`δ_k` 与风险账务可加总核对。
- **停止条件**：若出现任一退化或覆盖违规，停止并定位到族/记录/步，原样报告，不改口径。
- **成本**：按 `K` 由问题决定；先前测得按状态循环在第 5 步已收掉大部分闭合，故 `K` 不必大，但**不得**为省算力而先认定 `K≈6`。

## 5. 状态

- 本文**不含新实验**，不改任何封存，不改变任何任务的验收结论（② 行的修订只是把已发生的事实登记进来）。
- 仍未决：`L12S` 传播路径未修复（A 已执行、B 未启动，**暂缓是优先级决定**）；`FP-RERUN-001` 的 `H6` 为 INVALID（其本意已由 `FP-CLOSURE-001` 直接核查，**范围仅限计划参数与四探针**）；`H5` 为交付 PASS（每格每步预算**事后补入**）。

## 6. FP-COMPOSE-001 带来的状态变化（2026-09-14）

- **补上的**：② 那一格的接缝缺口。有限结论草案见
  [`finite_conclusion_draft.md`](research_branches/FP-COMPOSE-001/claude/finite_conclusion_draft.md)
  （Claude 起草，**待 GPT 采纳为正式结论**）。
- **任务状态**：用户裁决豁免交叉验证后记为 **`VERIFIED`（附单侧限制）**；
  **任务单文件本身仍显示 `ACTIVE`**，因为按 AGENTS §一/§四 任务定义的修订只能由 GPT 进行。
- **没有改变的**：③ 的成本普适优势仍**未**被支持；后段收益仍**未**回答；跨精度/跨种子稳定性仍**未**测；
  "误差不随维度增长"仍是"这两族上未观察到"；`L12S` 及任何传播构造仍**未**修复、仍**不得**依赖；
  跨记录/跨臂的联合风险仍**未**分配。**没有任何一条旧的撤回被本次结果恢复。**
- **唯一的验证缺口**：本任务**没有**独立作者的验证（用户已豁免）。C1 同执行者复现通过
  （Q̂ 重放最大差 `0.0`），但**同执行者复现不是独立验证**。
