# FP-ITER-001 execution plan

The frozen authority is research_tasks/FP-ITER-001.md v1.1. This plan adds no
scientific protocol or acceptance condition.

1. Preserve v1.0 history; record v1.1 read-only pre-review and its exact task
   digest. Resolve any task objection through the user before execution.
2. On APPROVED, commit ACTIVE task and common protocol on codex/FP-ITER-001.
   Create an isolated claude/FP-ITER-001 worktree from that same commit.
   Record the common commit and unmodified task digest in each route manifest.
3. Independently construct the literal network, scalar reference and proof;
   implement only the two actor-scoped Python files. Do not inspect the other
   implementation, theory or results before both first-result seals.
4. Each actor runs its verifier, fixed eight-sequence finite protocol and four
   reference sequences, then scoped lint. Preserve failures and corrections.
   Save strict JSON and environment/source evidence. Commit each blind seal.
5. Enter VERIFYING. Each actor reviews the other's frozen source and proofs,
   reproduces its scripts, and checks reconstructible numerical claims against
   the frozen protocol. Original authors repair implementation defects.
6. Record reciprocal PASS/FAIL/OBJECTION, synthesize only supported conclusions,
   update ACTIVE_WORKSPACE, and commit task-scoped results on codex branch.
   No main merge or push is part of this plan.

The user-facing answer should explain whether iteration converged, which
point it approached, what missing coverage changed, and the immediate
research decision. It must distinguish verified conclusions from preliminary
results. The user's three untracked learning documents stay untouched.
