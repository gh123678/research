# FP-COMPOSE-001 用户裁决记录：豁免本次交叉验证（2026-09-14）

- 记录者：Claude 执行方（被裁决事项的直接当事方，故只记录、不评价裁决本身）
- 分支：`claude/FP-COMPOSE-001`
- 任务单：`docs/research_tasks/FP-COMPOSE-001.md` v1，基线 `d175379`
- 相关产物：`first_result.md`（封存提交 `629d4fa`）、`verification_of_other.md`（阻塞记录，提交 `3580097`）

## 1. 裁决前的状态

| 项 | 状态 |
|---|---|
| 我这一侧（claude 路线） | 门禁 T1–T5 全 PASS；正式矩阵 96/96 记录、exit 0；`first_result.md` 已封存；C1 同执行者复现 **PASS**（Q̂ 重放最大差 `0.0`，E_Q `1.11e-16`，价值 `3.55e-15`，1536 个批哈希全一致） |
| 对方（codex 路线） | 工作树仍在基线 `d175379`，除未跟踪的任务单外**无任何产物** |
| 交叉验证 | **无法执行** —— `verification_of_other.md` 记为 `NOT PERFORMED (BLOCKED)`，并明确**不给出** PASS/FAIL/OBJECTION |
| 任务状态 | `ACTIVE`（单侧已交付） |

## 2. 提请裁决的争点

AGENTS §七规定：任务只有在**双方验证报告齐备**、或**用户明确裁决例外**后，才能成为 `VERIFIED`。
codex 路线不可用，因此交叉验证在可预见的时间内无法产生。请示三个选项：

- **A**：裁决豁免本次交叉验证，任务记 `VERIFIED`，并把"仅单侧证据"写进结论而非抹掉；
- **B**：维持 `ACTIVE`，按"单侧已交付、验证受阻"如实收尾，三段结论明确保持暂定；
- **C**：引入第三方验证者（记录中已说明：**我派出的任何子代理与我同模型、同代码来源，不构成独立**）。

## 3. 裁决

**用户裁决：A。** 用户以"好"批准了上述建议（对话于 2026-09-14）。

据此：

1. FP-COMPOSE-001 在**用户明确裁决例外**的条件下记为 **`VERIFIED`**（依据 AGENTS §七的例外条款）。
2. **该 `VERIFIED` 附带的限制条件必须随结论一起出现，不得省略**：
   - 交叉验证**没有发生**；本任务**只有一位作者的路线**。
   - 存在的是**同执行者复现（C1）**与**另写核的独立重算**，不是**独立作者**的验证。
   - 缺口的性质：C1 与独立重算能抓**算术/实现**错误（这一点已被证实有效——`analyze.py` 的路线覆盖缺陷正是被独立重算抓到的），但**抓不到共同的 conceptual 错误**——那需要一个不共享我实现与思路的第二作者。
   - 因此本任务的结论应读作"**在用户豁免下、单侧证据支持、且作者侧已完成同执行者复现与独立重算的有限结论**"，不得读作"经双方独立验证的结论"。

## 4. 治理后续（需要 GPT 或其可用执行者完成）

- **任务单本身仍显示 `ACTIVE`**。本裁决记录在**我自己的分支文档**与 `ACTIVE_WORKSPACE.md` 中；
  按 AGENTS §一/§四，**任务定义的修订只能由 GPT 进行**，我**不修改** `docs/research_tasks/FP-COMPOSE-001.md`。
  待 GPT 可用时，应把本裁决写入任务单 §11 并发布修订版本，使任务单的状态字段与索引一致。
- 本裁决**只豁免 FP-COMPOSE-001 的交叉验证**，不构成对其它任务、也不构成对 `L12S` 传播路径或任何已撤回保证的豁免。
- 向 `main` 合并仍需用户单独批准（AGENTS §九）；本裁决**不是**合并批准。

## 5. 我作为当事方的两点保留（记录，不改变裁决）

1. 我推荐的正是 A，但我仍要把它的代价写清楚：**单侧证据没有独立作者**这一缺口，在本项目里不是理论上的——
   `analyze.py` 的路线覆盖缺陷如果只靠自检就会漏掉，是独立重算抓到的；而更早的 `L12M`/`L12S` 两次撤回，
   是**外部审阅**抓到的，不是任何一次自检抓到的。若日后有条件补一次独立作者验证，它的价值高于再跑一个新环境。
2. 我这一侧在封存后自查出 §6 高精度诊断工具本身的缺陷（见 `first_result.md` §8）。该缺陷不影响封存实验，
   但它是我在这个任务里唯一一次"把未执行的断言写进封存报告"，已如实记录。它在方向上支持同一个判断：
   **验证强度应当与结论强度匹配**，而单侧证据不支持强结论。
