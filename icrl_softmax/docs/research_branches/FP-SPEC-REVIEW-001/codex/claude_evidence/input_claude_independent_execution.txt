主人，Claude 路线的两项交付物都已完成并封存。

**产出路径**

1. 独立报告
`C:\Users\Admin\Desktop\research\icrl_softmax\results\FP-SPEC-REVIEW-001\claude_worktree\icrl_softmax\docs\research_branches\FP-SPEC-REVIEW-001\claude\first_result.md`
2. 哈希/命令证据文件
`C:\Users\Admin\Desktop\research\icrl_softmax\results\FP-SPEC-REVIEW-001\claude_worktree\icrl_softmax\results\FP-SPEC-REVIEW-001\claude\evidence_and_commands.md`

**SHA256**

| 对象 | sha256 | 来源 |
|---|---|---|
| `Xie_2026_beyond_linear_attention.pdf` | `04aef63b86239100c0ec836e6105486db125a4b4ec1dbfdbc0138bfc2f9647d3` | manifest |
| `Xie_..._attention.txt`（分页文本） | `9026158eef03b6d5c8bd83f0742a99f30aa882b63c0d7e2cbbc220e7941134f7` | manifest |
| `Liang_Lai_2026_linear_attention_policy_improvement.pdf` | `6f579b4fdc818d493350878ba042bba6ae324d8631c8ed9a8341298c4eed7f9b` | manifest |
| `Liang_Lai_..._improvement.txt`（分页文本） | `bf254437a04af611d531b4f706a4d4093674684c6d15351880f5d67cb0ccbe52` | manifest |
| 代码基线 commit | `16ee0f652cde56b4f2ef6e3f1a43b58818bb0108` | 任务记录 |

主人，必须先坦白一件事：**这两份交付物自身的 SHA256 我算不出来**。本会话的 Bash 完全不可用——每条命令（连 `pwd` 都算）都在 harness 建 session 环境时失败：`EPERM: operation not permitted, mkdir 'C:\Users\Admin\.claude\session-env\1b89a67d-...'`，加不加 `dangerouslyDisableSandbox` 都一样。同会话内 Grep / Glob 未启用，`mcp__agentflow__*` 被权限拒绝，连记忆目录写入也被 EPERM 挡住。因此上表只能引用冻结 manifest 里已预审过的哈希值，而不是我本地重算的。此项已如实写入证据文件 §1.3 与 §4，并列出待补算清单——请主人让有 shell 的一方在 VERIFYING 前补上两份文件的哈希。

**审计结论摘要**（详见报告 §6–§10）

- 两个实现算子各自对应的是**本地声明的扩展**，不是外部论文。`H1` 满足（FP-ESARSA-001 冻结契约 + `fixed_policy_expected_sarsa.py` + 验证脚本构成可检数学规格），`H3` 满足（记忆、路由、采样、更新时序、logits、控制六类差异全部枚举并归因）。
- `H2` **在两处归属声明上不成立**，为本次审计的主要发现：
  - F1（中）：`model.py:518` 与 `model.py:926` 称等值 mask 对应"论文 Theorem 3.1"。但 Xie 全文只有 Theorem 1/2/3，**没有 Theorem 3.1，也没有等值 mask**（唯一的注意力 mask 是 p.3 eq.(1) 的固定 query-column mask）；Liang-Lai 的 Theorem 3.1（p.4，eq.(5)）是线性注意力 + **采样**动作，**根本不存在 mask**。两篇都无法支持该归属。
  - F2（中）：`model.py:633` 称 gate 是"论文声明的 visited-query gate"，两篇均无此声明。
- 其余为低严重度或非问题：F3（构造文 §7 是 gate-assisted 且缺 `log π` 偏置，实现是 gate-free 且带 `log π`；文档的界因 `|S|−1 ≤ |S||A|−1` 仍 a fortiori 成立）、F4（精确路线的 "Softmax" 只是名义，其 softmax 全部退化）、F5（收缩前提 = Xie Assumption 5.1 提升到配对核 `M_π^X`，2C 保守量已声明）、F7（构造文 §8 自己警告字面块与紧致算子"不能互换"，被审类属后者）。
- **无任务级 OBJECTION。**

未做且按指令不做：未读 Codex 路线的任何结果或结论，未做互审，未跑实验，未改 `model.py`，未改任务定义，未 push / merge。
