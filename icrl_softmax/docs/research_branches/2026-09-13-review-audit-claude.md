# 2026-09-13 GPT 审阅意见的 Claude 独立核查存档

日期：2026-09-13。分支：`claude/FP-CENSUS-001`。基线：`2e4c5ec97c24205f3cab8ee653e87b883c4660db`。
执行者：Claude（独立验证者角色）。性质：对 GPT《科研工作审阅与规划（2026-09-13）》的**只读独立核查**加用户授权的勘误执行；本文件不发起正式任务，不升级任何任务的验证状态。

方法：逐项打开审阅引用的代码与报告位置核对；对统计类主张用封存 JSON 独立复算（脚本为一次性只读命令，未改动任何结果文件）。未运行新实验。审阅中涉及文献定理前提（Maurer–Pontil 适用条件）的部分本次未做文献核查。

## 一、核查裁定汇总（12 项抽查，全部属实）

| # | 审阅主张 | 裁定 | 证据 |
|---|---|---|---|
| 1 | 旧 frozen 证书均值步把估计尺度 `s_x` 代入需要值域参数的 Hoeffding | 属实 | `fixed_policy_variance_certificate.py:175-176`：`radii = 2*s_x*sqrt(log_b/n_b)`；残差真实值域为 `4B=20`，`s_x≈1.2` 是估计的二阶矩尺度。二阶矩上界不蕴含同尺度次高斯性 |
| 2 | 新 Bernstein 实现二阶矩余量少一个 `√2` | 属实 | 旧 `fixed_policy_variance_certificate.py:171`：`(E²/2)*sqrt(2*log/n) = E²*sqrt(log/(2n))`（正确 Hoeffding）；新 `fixed_policy_bernstein_certificate.py:263`：`(E²/2)*sqrt(log/n)`，恰为旧式的 `1/√2`。"该步保持不变"不成立；由此"只换均值不等式带来 15% 收益"的单因素归因亦不成立。注意方向：新余量**过紧**，即实际保证比宣称的更弱 |
| 3 | FP-RANGE 把截断变量样本方差代入普通 Bernstein | 属实 | `fixed_policy_bernstein_certificate.py:416-417`：`var_t`（样本方差）直接进入普通 Bernstein 不动点 `_equilibrium`，无 Maurer–Pontil 经验修正项（同文件 `:90-91` 自己写明该项的形式） |
| 4 | 多步复用同一认证集，引入原证明未处理的适应性；首次弃权即 break | 属实 | `evaluate_fp_iter8x_001.py:120` 循环外只生成一次 `cert`；`:144` 每步复用；`:191` 下一步策略来自该 cert 筛选的 eta；`:193-194` 首次不发出即 break。"发出群体不回升"是程序不变量 |
| 5 | `pi_min=0.15` 仅作用于初始策略 | 属实 | `fixed_policy_expected_sarsa.py:481-490` 的 `relative_softmax_candidate` 无保底/混合/投影。总览把间隙不归零归因于"策略类受 pi_min 限制"不成立 |
| 6 | `841` 实为 `837` | 属实 | FP-ITER8X 报告自身表格列出 `402+435`（=837），同文两处与任务单两处写 `841`；FP-ATTN-8X 报告写 `837`。系汇总错误 |
| 7 | "每步收掉 45%"应为约 31%，下游未同步 | 属实 | FP-GAP 报告已局部勘误：`0.45 = (1−ρ)/ρ`，`ρ≈0.69`，`1−ρ≈0.31`，且 `1/1.45=0.690`、`0.45/1.45=0.310` 算术自洽；但总览仍沿用旧表述（本次已修） |
| 8 | "第 18 步保证蒸发"的地板含人为阈值 | 属实 | `evaluate_fp_meaning_001.py:50` 值迭代停止阈值 `1e-14`；FP-MEANING 报告 §2 自己承认地板由该阈值主导。两种价值算法之差不是 LB 判决链的前向误差上界 |
| 9 | "第 17 步网络不可信"比较了错误的量 | 属实 | FP-ATTN-8X 报告 §5 的 headroom 为"跨记录最小真实增益 ÷ 跨记录最大 float32 估计差"，未把误差传播到判决量 `LB_s = Î_s − E_Q‖Δπ_s‖₁`。报告虽标注"外推"，被比较的量本身仍不是判决量 |
| 10 | SHORT 诊断换了总体却沿用旧解释 | 属实 | `analyze_fp_short_001.py:172-173` 只筛 `frozen_grid` 单臂且 `emitted ≤ 5`（含 0 步）；SHORT 报告 12 条中恰有 6 条 `emitted=0`。GAP 的 12 条为 96 条轨迹中发出 1–5 步者（另有 10 条零发出单独计数）。两个"12"不是同一群体 |
| 11 | `54.89%` 写反 | 属实 | FP-GAP H6 表头为 "mean fraction of the gap **closed**"（`54.89%` 是已收回）；FP-SHORT 开头写成 "leave `54.89%` **uncollected**"，方向相反 |
| 12 | "中位数 1.121 / 需再降 12%"用的是上中位数和比值口径 | 属实 | 用 SHORT 封存 `task_results.json` 的 12 个 `e_q_over_h` 复算：真中位数 `1.103873`（全 12 条）、`1.029009`（实际发出 1–5 步的 6 条）；正确口径 `1−h/E_Q` 的逐记录降幅中位数 `9.39%`（全 12 条）、`2.77%`（6 条） |

## 二、封存数据复算记录（可复核命令与数值）

- **FP-SHORT-001**（`results/FP-SHORT-001/claude/formal/task_results.json`）：12 条短记录的 `e_q_over_h` 排序后取真中位数 = `1.103873`；1–5 步 6 条 = `1.029009`；逐记录 `1−1/(E_Q/h)` 中位数 = `9.3879%` / `2.7720%`。与审阅给出的 `1.103873 / 1.029009 / 9.39% / 2.77%` 完全一致。
- **FP-HORIZON-001**（`results/FP-HORIZON-001/claude/formal/task_results.json`，frozen 臂逐步总体最小 `min_lb`，32 步）：相邻比值算术平均 = `6.54`（与 FP-MEANING 报告一致），回升 6 次，比值跨度 `0.000756`–`70.726`，首尾几何均值 = `2.468`。即"固定每步衰减 6.54 倍"的读法不成立，审阅给出的修正数值精确。
- 以上复算均为只读操作；未改动、未重新生成任何封存文件。

## 三、本次已执行的勘误（用户 2026-09-13 授权，全部保留原文痕迹）

1. `docs/research_branches/FP-ITER8X-001/claude/first_result.md`：三处 `841 → 837`（含勘误注记）。
2. `docs/research_tasks/FP-ITER8X-001.md`：结果区两处 `841 → 837`（含勘误注记；未触碰任务定义、假设与验收条款）。
3. `docs/research_branches/FP-SHORT-001/claude/first_result.md`：`54.89%` 方向修正；"中位数 `1.121`"修正为真中位数 `1.103873`；"约 `12%`"三处修正为 `9.39%`/`2.77%` 口径；新增 SHORT 与 GAP 总体差异说明。
4. `ACTIVE_WORKSPACE.md`：`841 → 837`；FP-GAP 两处 45% 表述改为正确口径；`pi_min` 归因撤回；`6.54` 读法勘误；"第 17 步可信地平线"与"第 18 步保证蒸发"两条结论**撤回并改为待检验**；HORIZON `H4` 标注为程序不变量；顶部索引修正（原长期指向 ITER6）；新增 2026-09-13 勘误与核查摘要。
5. `docs/research_branches/FP-MEANING-001/claude/first_result.md`：§4 增加勘误块（`6.54` 为算术平均、几何均值 `2.468`、"实测地板"非 LB 前向误差界、第 18 步穿越结论撤回）。

## 四、未核验事项（留给正式阶段任务）

- Maurer–Pontil 经验 Bernstein 定理前提与实际采样协议（随机计数、Markov 链前后半划分、多步适应性）的逐条比对——需阶段 2 正式任务中的推导与文献核查。
- 封存 HORIZON 中"1578 次更新、24 次 eta<1"的计数复算。
- 网络路线 12 步一致性的独立重跑（本次未重跑任何实验）。
- 完整新颖性文献检索。

## 五、Claude 的总体意见（与审阅结论一致）

抽查 12 项零失误。第 1–4 项为推导/实现层面的实质问题，足以支持审阅核心判断：**"多步认证策略改进"的概率保证当前未闭合，零观测违规不构成证据**（FP-TIGHT 已证明不成立的臂也能零违规）。同意"暂停延长 horizon、优先修复证明与勘误"的排序。补充：第 2 项的余量缺口方向是**过紧**（保证比宣称更弱），建议阶段 2 重推时列为第一项。

阶段 2（证书重推、随机计数、多步数据复用条件）属于任务级工作，须由 GPT 发起正式任务并经 Claude 预审；本次勘误不构成对该阶段任何方法选择的预先认可。
