# 待用户裁决的具体修订（尚未生效）

背景：Claude 对 GPT 起草的补审任务提出 O1/O2 任务级 OBJECTION。依根 AGENTS.md §四，受影响的 R/D 暂停；T/G 继续。本文件是送裁决的修订提案，不是已发布任务定义。

## 建议采用
1. O1：禁止导入 docs/research_branches/FP-COMPOSE-001/claude/ 和 FP-COMPOSE-002/claude/ 下任何程序（包括间接导入）；只允许调用定义待测对象的基线 model.py、数组估计器、MDP/训练生成器及它们必要的基线依赖。证书、认证采样、首访保留、门限、判决与价值核均由 GPT 独立实现。记录实际共享依赖清单及 SHA256。
2. O2：补入 001 的 formal/task_results.json、input_manifest.json、verification/selfcheck_c1.json（若实际名称不同则记录准确位置）；同时保留 002 的全部封存输入。先核对 001 与 002 前四步的输入、元数据及输出；在输入逐位一致的范围，完整重跑 002 的前四步同时对照两套封存结果，不重复算作两次独立运行。若不一致，单独记录并停止受影响路线，不能默认为等价。覆盖目标保持 002 的全 96 条记录、1136 个唯一认证批、2267 次网络前向、4534 个生产者步骤，兼核 001 的 1536 步；不新增实验臂。
3. O3：首个固定输入的单线程/6线程桥接如差 >1e-9，正式重放必须用 6 线程；后续任何单线程重放差 >1e-9 也必须先回到 6 线程核查，不能以线程差异直接豁免。可以直接全程 6 线程而省略单线程尝试。
4. O4：加入 dimension_scaling.json/md 的源哈希；正式执行前冻结任务单哈希、精确基线及命令。Claude 最终检查是对 GPT 审阅的交叉检查，不是对其自身原实验的独立佐证。

不变：原冻结种子、K、链数、网络、判决规则、数值门槛、资源上限、原作者文件只读、禁止合并 main。

用户可批准上述四条；或指定 O2 改为只审 001 理论文档与 002 数据矩阵。未经用户裁决不修改正式方法并启动 R/D。
