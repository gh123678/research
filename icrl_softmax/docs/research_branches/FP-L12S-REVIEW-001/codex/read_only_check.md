# GPT 只读核查记录
日期：2026-09-14。基线：05f59890f9d00e3de831beea1c13431a5fa11d17。
本记录由 GPT 在等待全新 Claude 审查时独立形成；未将这些结论提供给审查者。

## 证明条件
fixed_policy_tight_certificate.py:392–415 在闭包 estimate 内反复复用 counts_b；400 行初始化 v，403–411 行递推。即使额外假定 A 与 B 真正独立且给定所有样本容量，V0 为常量，V1 可以不依赖 B 的后继值，但一般非恒定的 V1 经 T_B 作用生成 V2 后，V2 依赖 B；下一次使用 T_B(V2) 已不是对 B 独立的固定函数。修复文档 §2 的“每个 V_k 都固定”不成立。只做 A/B 二分无法支持现有逐步 Hoeffding 证明。这是证明缺口，不能仅据此断言每个数值界必错。
此外首访按每对保留次序切分，不能直接从每对条件 iid 推出全体 A 与全体 B 独立；不同对的样本可出自同一条链，需另证共同条件化。
最终 estimate(v) 仍使用 log_k；log_f 已计算但未使用。n_iter=12 时相对登记最终风险更保守，单独看这点不是反保守错误，但文档风险等式与实现不完全一致。

## 封存统计直接重算
从原始 task_results.json 汇总全部 48 条路线记录，无需调用原分析器。结果见宿主 results/FP-L12S-REVIEW-001/codex/statistics_check.json，内含输入 SHA256。frozen/L12/L12S/L123 发出总数分别为 36/66/60/110，均收益 1.73409697/2.37705097/2.25897808/2.95615844，与报告相符；这仅验证封存统计，未重新生成 Qhat、采样或真值。
所谓同总数据截断实际为逐记录以 frozen 模拟步数封顶。截后实际数据：frozen=88,080,384；L12=87,031,808；L12S=85,983,232。故应称同一数据预算上限，非严格相同实际用量。截后 49/43 次发出与 2.11754953/1.93020249 收益均复现。该差别不推翻预算上限下的这组测量排序。
报告 H5 把任务的成本报告要求改写成收益不劣阈值，不能当作原预登记验收。
H6 的 60<=99 不能证明批次一致，也没有跨适应性轨迹的必然单调性；需检查随机种子协议或批次哈希。
H3 只否定这个总体、配置与协议中的总发出数假设；不能据此关闭所有传播方法。原始实际用量 L12S 小于 L12，也不支持成本/收益意义的全面支配。

## 边界
保留测量证据；不接受 L12S 理论保证已修复的声明。未执行修复或实验，不升级 VERIFIED。等待 Claude 具体审查结果及必要的用户裁决。
