# 收到外部审阅及作者回应（FP-COMPOSE-002）

- 审阅范围：提交 `bcec1fe`，重点为 `FP-COMPOSE-002` 的实验、分析程序与扩维报告
- 审阅方立场自述：**未调用作者的新增分析器**，从封存记录独立重算；**未修改任何文件**；
  **没有重新采样或重跑网络**，故自述"不构成完整独立复现"；远端状态因 SSH 权限未能确认
- 作者回应：Claude（本任务的任务单起草者、执行者与验证者——**三者同一**，见任务单 §8）
- 处理原则：**先复算核实每一条，再改**。不接受也不辩解

## 1. 审阅结论原文要点

> 最近的报告需要修正；核心安全观测仍获支持，扩维上限和"尾部已经收敛"的结论不成立。
>
> **建议先由原作者修正分析器、报告和索引，再据此设计扩维实验。**

## 2. 逐条核实与处置（全部经我复算）

| # | 审阅指出 | 我的复算 | 处置 |
|---|---|---|---|
| 1 | 后段贡献少算一步：6–12 步应为 **4.309** 点（非 `2.601`），5–12 步应为 **7.839**（非 `4.309`）；下标错位 | **确认**。`mean_by_step[i]` 是第 `i+1` 步**之后**的值，故 `a..b` 步的贡献是 `mean[b−1] − mean[a−2]`；我写成了 `−mean[a−1]`。实算 `mean[11]−mean[4]=4.3093`、`mean[11]−mean[3]=7.8391` | **已修** `analyze.py`：新增显式 `contrib(a,b)` 并加下标注释；重跑，`4.3093`/`7.8391`。**判定不变**（`≥2.0`，仍"不可忽略"）。`first_result.md` §0/§1/§5.2 已改并留更正说明 |
| 2 | 扩维"上下界"无依据："维度增大必然使方差、价值范数增大、门限下降"未被证明 | **接受**。四项中**只有 `E[max]` 随 `d` 增长是数学上被强制的**；方差、`‖V̂‖`、`h` 三项都是**我证明不了的猜测**。把猜测当界的定向＝把情景外推冒充有保证的区间 | **已撤回**。`dimension_scaling.md` §5 改为逐项标注"性质"（成立 / 猜测未证明），删除"上界/下界""天花板低于 `d≈64`"等句；改为"情景预测，方向未被建立" |
| 3 | 小增量推不出"没有可改进的量"；反例 `f2/0.2/task313/expected_finite/numpy` 第 12 步最小增量 ≈`1.10e-7`，却只闭合 **62.68%**，尚余 **37.32%** | **确认，精确复现**：`closure=0.6268`、`gap_denom=4.6151`、最小增量 `1.095260e-07` | **已撤回** `first_result.md` §4 的"数值零 / 已经被耗尽"整段解释，并把反例写进去。**性质是逻辑错误而非算术错误**：把"**某一个状态**的增量≈0"当成"**这一格**没有可改进的量"。**修正后的说法：小增量不蕴含已收敛**；且该反例说明**可改进的量还在，是证书/门限在限制推进** |
| 4 | "192/192 每一步都有正向收益"口径错误：程序查的是**累计**收益 | **确认**。逐步增量为正的记录数：`192/192` 至第 5 步，之后 `191, 188, 186, 185, 185, 180, 180`（第 6…12 步）——与审阅给的"第 6 步 191/192、第 12 步 180/192"**完全一致** | **已修** `analyze.py`：两个口径**分开命名**（`share_cumulative_positive_by_step` 与 `share_with_positive_STEP_increment_by_step`），并注明**引用后者**；`first_result.md` §1/§5.2 已改 |
| 5 | 预登记判定未严格执行；`12±3` 含第 13–15 步，只观察到第 12 步不能单独证伪该子预测 | **接受**。我此前把整条 `P2` 笼统记为"未命中"是**推断错误**：观测"12 步内未跌破"**推不出**"13–15 步内不会跌破" | **已修** `first_result.md` §0：拆为 `P2a`（速率，**FALSIFIED**）、`P2b`（窗口，**尚未完整观测，不得记作未命中**）、`P2c`（命中）；§1 的 H2 行改为"已否定部分记 FALSIFIED；未观测部分单列" |
| 6 | 记录错误：唯一认证批实际 **1,136**，报告误写成上限 **1,152** | **确认**。`cost['unique_batches']=1136`，且 `1,191,182,336 / 1,048,576 = 1136.000` 恰为整数；差额正是**停止的格不再抽新批** | **已修** `first_result.md` §2，并加更正说明 |

## 3. 审阅方核实**成立**的部分（我逐项对过，全部一致）

| 审阅独立重算得到 | 我的封存记录 |
|---|---|
| 前四步 **1,536** 个步骤、**16** 个字段完全一致 | 一致（`H0` PASS） |
| 全部 **4,534** 步：覆盖违规 **0**、退化 **0** | 一致 |
| **192** 对生产者路线的判决序列与逐行 `η` 一致 | 一致（`H4` PASS） |
| 第 12 步总体平均闭合率 **89.3123%** | 一致（`0.893123…`） |

**这四项的分量**：它们是**在未使用我的分析器**的情况下重算得到的，
因此**独立于我的分析代码**。这正是本次审阅最实质的贡献之一。

## 4. 这次审阅改变了什么

1. **两个实质结论被撤回**：①"尾部在被认证的是数值上不存在的改进 / 格已被耗尽"；
   ②扩维的"上界/下界"定向与"天花板低于 `d≈64`"。
2. **一个可操作的错误被纠正**：`d≈96` 的预算结论。我此前只算链数（1.0–1.8×）
   就断言"远在 2 小时内"，**漏掉了前向成本随 `d` 增长**（前向在 `n_pairs=d` 个 token 上做）
   ——按实测 `1.45 s`/前向（`d=24`）外推，`d=96`/`K=12` 约 **3.7 h，超出 2 h 上限**。
   **在 smoke 标定前不得再声称这一步便宜。**
3. **保留下来的核心安全观测**：`E_Q ≥` 实测误差、逐分量不退化、生产者判决对应、
   `H0` 逐位复现——这四项**未被本次审阅动摇**，且被独立重算确认。
4. **被强化的方向性结论**：问题 3 的反例（`task 313` 尚余 37.32% 却给出≈0 的单状态增量）
   说明**是门限/证书在限制推进，而不是"没有可改进的量"**——这与扩维分析
   "**门限先关、支持度后崩**"方向一致。但**这是两条独立论据，不用后者替前者背书**。

## 5. 这次审阅**没有**覆盖什么（我如实标注，不借它升级证据等级）

- **不是完整独立复现**（审阅方自述）：**没有重新采样、没有重跑网络**。
  因此**证书输入端**（新批次、首访统计量）与**网络前向**没有被独立重算。
- **定理层未被审**：T1–T5、`L12` 单侧证明、条件性不退化证明**不在本次范围内**。
  这仍是风险最高、而重算最抓不到的一层。
- **扩维分析的全部数字仍未被独立复算**（只审了"上下界"这一定性越界问题）。
- 远端状态未确认。

## 6. 我对这份审阅的评价（作者视角，仅记录）

它**做到了我自己做不到的事**：在没有我的分析器、也没有我的推理的情况下，
用封存记录重算出四个与我一致的正面结论，并在同一批记录上**找出我五个错**。
其中问题 3 与问题 5 是**概念/逻辑错误**——**重算一万次也抓不到**，只有换一个不共享我思路的读者才会暴露。
问题 1 与问题 4 是**我自己的分析器**的口径错误，而审阅方**没有用我的分析器**，所以是真正独立的发现。

这直接印证了我在 `first_result.md` §8 写的那句话：**唯一还没买到的，是一个不共享我思路的读者。**
本文件是那个缺口被部分补上的一次记录。**但按 §5，它补的是分析层与报告层，不是定理层。**
