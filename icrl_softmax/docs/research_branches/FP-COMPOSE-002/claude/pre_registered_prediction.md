# FP-COMPOSE-002 预登记预测（**正式运行之前**冻结）

- 任务单：`docs/research_tasks/FP-COMPOSE-002.md`（Claude 起草，用户例外授权）
- 冻结时刻：**在任何 `K=12` 数据产生之前**
- 依据：`docs/research_branches/FP-COMPOSE-001/claude/headroom_analysis.md`
  与 `results/FP-COMPOSE-001/claude/formal/task_results.json`
- 性质：**外推与预测，不是测量。** `h` 与 `E_Q` 都是策略轨迹的**非线性**函数，趋势**可能改变方向**。
  写下它们的唯一目的是让"事后解释"变得不可能。

---

## P0 协议复现（可判定，非统计）

`K=12` 运行的 **`step = 1..4`** 必须与 FP-COMPOSE-001 的封存结果**逐步逐位一致**。

**为什么应当如此（不是巧合）**：`δ_step` 在本次保持不变（`0.0125`），认证批种子向量
`[20260911, 90417, round(100·mixing), task_index, step]` 按 `step` 索引，链数（16,384）与链长（64）不变，
训练批与初始策略不变，`y_range` 公式不变。故 `step=1..4` 的**输入完全相同**，判决是输入的确定性函数。

**判据**：`pi_before`、`q_hat`、`pair_sizes/means/vars/radii/eps`、`e_q`、`y_range`、`lb_table`、
`rows_eta`、`update_mask`、`pi_after`、`audit` 全部一致。任一不一致 → **FALSIFIED**，
且按任务单 §6 视为**实现或环境缺陷**，不当作科学结果。

## P1 首个停点（对应 H2）

- **预测：至少一格在 `step ≤ 6` 停止发出。**
- **首选观察对象**：`f1 / mixing 0.5 / task_index 207 / expected_exact / network`
  ——它是 `K=4` 上最紧的一格（`step 4` 时 `h = 0.2775`、`E_Q = 0.2551`、`h/E_Q = 1.0876`），
  按 `−9%/步` 它应在**再 1 步左右**关闭。

## P2 余量轨迹（对应 H2）

| | 预测 |
|---|---|
| `h/E_Q` 中位收窄速率 | **约 `−9%/步`**（`K=4` 实测 f1 `×0.709`、f2 `×0.687`，即 `−8.7%`/`−9.5%`） |
| f1 中位首次 `≤ 1` | **`step = 12 ± 3`**（从 `step 4` 的 `1.990` 起算，`ln(1.990)/0.09 ≈ 7.6` 步） |
| f2 中位首次 `≤ 1` | **在 `K=12` 内不跌破 1**（从 `step 4` 的 `3.015` 起算需 `ln(3.015)/0.09 ≈ 12` 步，落在边界上，故这是**较弱**的一条） |
| `E_Q` 自身 | 涨幅温和：`K=4` 实测只涨 `×1.056`（f1）/`×1.174`（f2）；预测 12 步内 `×1.3` 量级，**不是**指数爆炸 |

## P3 后段贡献（对应 H3）

用**固定分母 192** 的总体闭合率曲线（停止轨迹保持末值，无删行）：

| 量 | 预测 |
|---|---|
| `k ∈ [6,12]` 的贡献 | 落在 `[1.0, 8.7]` **个百分点**内 |
| 我的中心估计 | **约 2–5 个百分点**（低于无停止外推的 `8.7`，因为 P1 预测有格会停） |
| 判据（任务单 §3 H3） | `≥ 2.0` 点 → "非零且不可忽略"；`< 1.0` 点 → "可忽略"；之间 → **如实报为不确定** |

**推导（供复核）**：FP-COMPOSE-001 的 D3 每步增量 `0.3394, 0.2570, 0.1467, 0.0717`，
比值 `0.757, 0.571, 0.489`。按比值 `0.55` 外推 `k=5..12` 增量约
`0.0394, 0.0217, 0.0119, 0.0066, 0.0036, 0.0020, 0.0011, 0.0006`，合计 ≈ `0.0869`（`8.7` 点）——
这是**停止数为零**的上界。

## 我**不**预测什么（避免事后自利解释）

- 不预测**哪一族的后段更多**；
- 不预测**总闭合率的上限**（`K=4` 已到 `0.8147`，12 步到多少我没有依据）；
- 不预测 `δ_total=0.05` 读法下会不会有格停止（该读法的 `E_Q` 更大，**更可能**停，但我没有它的实测）；
- 不预测生产者对应（H4）——它是确定性的，`K=4` 已 `192/192`，我预期仍成立，但这**不构成预测**。

## 事后必须逐条对照

运行结束后，`first_result.md` 必须**逐条**给出 `P0`–`P3` 的**命中/未命中**，
**未命中要写在最前面**，并附上实测值。**不得**在事后修改本文件（只允许追加对照表）。
