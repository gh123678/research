# FP-KERN-002 Claude read-only pre-review

Date: 2026-09-09

Reviewed commit:
`f72d4fec81bf43f2efe39a770afad68f63d559d9`

Claude Code: 2.1.138

Tool boundary: permission mode `plan`; only `Read`, `Glob`, and `Grep` were
available. No Bash, Python, Write, Edit, Git mutation, web access, or
experiment was allowed.

## Outcome

`APPROVED`

## Itemized checks

1. Research question and falsifiability: `PASS`.
2. Exact source identity and no-new-trajectory boundary: `PASS`.
3. Stage 0 reproducibility from the declared saved fields: `PASS`.
4. Route definitions and oracle separation: `PASS`.
5. Observable partition determinism, missing distances, ties, and permutation
   behavior: `PASS`.
6. Metric and common-set definitions: `PASS`.
7. Ordered conclusion-rule exclusivity and completeness: `PASS`.
8. GPT, Claude, common-input, and prohibited-path boundaries: `PASS`.
9. Fixed smoke and sole full-analysis rules: `PASS`.
10. Independent construction and reciprocal verification: `PASS`.
11. Acceptance, failure, and stopping conditions: `PASS`.
12. Internal consistency across task, design, plan, workspace, and branch
    identities: `PASS`.

Claude confirmed that the eight possible hidden-gate/current-family patterns
are covered by the ordered rule, that the ten balanced partitions are
complete, and that only an observable-route pass can produce a `PROMISING`
classification.

## Nonblocking cautions to close before activation

1. Pin aggregation, tie exclusion, denominators, bins, and zero-denominator
   behavior for ARI, peer precision, and oracle-benefit recovery.
2. Replace the stale metadata phrase saying activation was absent while
   `DRAFT`; the task is now `REVIEW`.
3. Restate the requirement of at least two common positive-count non-target
   actions in observable distance construction.
4. Let deterministic verifier fixtures, rather than the data-dependent
   16-record smoke subset, guarantee coverage of `partition_tie`.
5. State explicitly that conclusion rules 4--6 refer to the hidden family.
6. Assign the one-time activation copy of the common input to GPT and freeze it
   immediately after manifest creation.
7. Serialize `oracle_generator_cluster` on `current_unstructured` as explicitly
   not applicable so independent bundle schemas reconcile.

These cautions do not alter the source corpus, three routes, neighbor count,
partition count or balance, missing-distance rule, metrics, primary thresholds,
or ordered scientific conclusion.

APPROVED
