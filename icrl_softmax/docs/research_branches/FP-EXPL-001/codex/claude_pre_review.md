# FP-EXPL-001 v1.1 只读预审报告

预审对象：任务单 v1.1（基线 ff8b6b0）、设计文档、执行计划、AGENTS.md 与工作区当前段落。本次为任务定义审查，未执行任何实验、未生成样本、未写入文件。

## 逐项检查

### 1. 输入与协议完整性 — 通过
- 全部数值输入冻结且自洽：gamma=0.7、alpha=0.5、target_pi、behavior_pi、rewards（含 (1,1) 改为 0.25 的事前非退化理由）、P_next_state、initial_state=0、batch_length=64、seed=20260911、Q0=0、updates=64、xi=zeta=tau=8.0。
- 采样协议逐转移规定两次 rng.random() 调用的顺序与阈值，禁止补样/洗牌/换种子/采到覆盖才停；要求双方独立生成后轨迹完全一致，并记录 Python/NumPy 版本。无留给执行者自由选择的参数。

### 2. 完整覆盖检查且无重采样 — 通过
- H1 明确为可失败诊断而非假设；覆盖失败输出 COVERAGE_FAILURE、原始轨迹与缺失项并停止，不得自动扩大样本，后续由用户裁决。固定长度采样，条件性结论的表述要求明确。

### 3. 字面有限注意力可行性与静态掩码 — 通过
- 精确参考允许声明的相等性掩码；有限网络禁止内容相等掩码，one-hot 指标只作点积描述。允许静态角色/位置掩码、prompt 初始化、固定位置输出提取；动态 Q 读写必须经字面矩阵；权重只依赖维度、target_pi、gamma、alpha、锐度，不得依赖样本/奖励/Q/真实 P。允许沿用已 VERIFIED 的 FP-ITER-001 结构，构造可行性有前序基础。target_pi 无零概率项，log target_pi 有限。

### 4. 独立标量/直接参考 — 通过
- 直接参考独立计算分组残差、不调用注意力实现；独立标量有限公式验证同一有限轨迹而不增加实验输入；GPT 独立重建全部算子与轨迹，不导入 Claude 算子作为审核依据，并只读审查源码排除外部 Q 查表与隐含 oracle。

### 5. 行为策略与目标策略分离 — 通过
- 采样用均匀 behavior_pi（阈值 0.5 一致）；后继平均严格用 target_pi 且禁止替换；P 仅供采样器与审计器，q_pi 仅供审计，均不进入网络。

### 6. 误差/不动点/收缩声明 — 通过
- c0 = 1−alpha(1−gamma) = 0.85：经核对，G0 = (1−alpha)I + alpha·gamma·W0·S0，W0·S0 行随机，故任意覆盖批次均有 ||G0||_inf ≤ 0.85，该界为先验可证而非事后拟合。
- 阶段误差分解逐项核对为恒等式：Ff(q)−F0(q) = −alpha·W(C−C0)q + alpha·gamma·W(S−S0)q + alpha·(W−W0)(r+gamma·S0·q−C0·q)。✓
- 扰动递推 E_(k+1) = c_f·E_k + 单步偏差 利用两算子的仿射性成立，且不要求 c_f<1 才有意义（c_f≥1 时界增长但仍有效），与"不代表发散"的表述一致。
- 审计界 ||q_hat−q_pi|| ≤ ||F0(q_pi)−q_pi||/(1−c0) 由 c0 收缩性标准推出，且明确标注使用审计真值、非学习器可计算证书。
- 三项分解、固定点残差界、c_f^k 衰减仅在 c_f<1 时适用；不适用项写 null 并说明原因；不默认逆矩阵存在。
- H5 的非退化论证成立：若 V 为常数 v，则 r_bar(s) = v(1−gamma) 必须状态无关，而 r_bar = (0.625, 0.25) 不同，故真实状态价值不可能相等。数据误差是否非零如实报告，不设为成功条件。

### 7. 证明与数值证据区分 — 通过
- rho(Gf) 仅作数值诊断；浮点特征值不得单独证明谱性质、唯一不动点或收敛；唯一性论证仅在 c_f<1 时验证。容差体系（1e-12 绝对；1e-10·(1+max_abs) 比例；不等式容差加右侧；原始抽样双路线完全一致）与 float64、64 步迭代、锐度 8 的误差量级相容。

### 8. GPT 独立验收的实质性 — 通过
- 验收程序独立重建种子、全部轨迹、C0/S0/W0/C/S/W、仿射算子、全部 Q 迭代、误差/界与适用固定点，并核对字面投影；先重放三条固定命令再运行 verify_claude.py；另有源码与证明的只读审查。用户裁决豁免双路线完整盲态实现已在任务 §1/§8 明确记录，保留 Claude 预审、原作者修复、GPT 独立验收及 Claude 对验收证据的复核，符合裁决范围。

### 9. 授权输出路径 — 通过
- Claude/GPT 产物分别位于 docs/research_branches/FP-EXPL-001/{claude,codex}/ 与 results/FP-EXPL-001/{claude,codex}/，与 AGENTS.md 的分支/目录隔离规则一致；隔离工作树、execution_manifest 的提交/SHA-256/环境记录、未发布 ACTIVE 前无有效执行基线等防护均已规定。禁止改动历史代码/结果、用户学习文档、main 与全局配置。

### 10. 生命周期与停止规则 — 通过
- 状态流 DRAFT→REVIEW→ACTIVE→VERIFYING→VERIFIED 与 AGENTS.md 一致；实现失败返回 ACTIVE 由原作者修复，定义异议进入 BLOCKED_BY_OBJECTION；VERIFIED 条件逐项列出；任何 main 合并另需用户批准；超时记录交接而非扩充协议；负面结果（覆盖失败、收缩充分条件不成立、数据偏差偶然为零）的处置规则明确，且禁止把"充分条件不成立"扩大为"不收敛"。外发授权阻塞已由用户"继续"裁决解决，与 §8 记录一致。

## 结论

未发现任务级实质缺陷。科学协议自洽、数学声明经核对成立、验证与验收路径可检查、负面结果处置和用户裁决范围记录完整。可以进入 ACTIVE 发布与 Claude 主执行阶段（在 GPT 完成 ACTIVE 发布提交之后）。

APPROVED
