# FP-INDEP-001 首次结果 —— Claude 路线（封存件）

- 任务单：[`docs/research_tasks/FP-INDEP-001.md`](../../research_tasks/FP-INDEP-001.md) v4（第四轮预审 APPROVED 后 `ACTIVE`）
- 基线：`1da7162abb662142332eaf4493209f73f41fb5c4`
- 路线：`claude/FP-INDEP-001`
- **本件先封存并记录哈希，之后才读取对方（codex）路线的任何内容**

---

## 1. 结论摘要（逐项，分开计分）

**限定结论在本路线完全复现，且是在自足实现上复现的**——生产路径**没有 import 任何基线模块**，
全部算子按任务单 §10 规格附录重写。

| 项 | 判定 | 证据 |
|---|---|---|
| **P1 安全失败检测** | **PASS** | 1,620 个已评估步：覆盖违规 **0**、逐分量退化 **0**；最小安全余量 **`+0.05273`**；最小价值增量 **`+6.074e-10`**（见 §4 的如实说明） |
| **R1 限定结论（numpy 格）** | **PASS** | 闭合差 `0.5648`（带 ≥0.40）；首步 `A 13 / B 29 / C 46`；`win 46 / loss 0`；救出 `33/35` |
| **R1 限定结论（network 格）** | **PASS** | 与 numpy 格**同一组数字**（见 §3） |
| **R2 生产者判决对应** | **PASS** | 288 条轨迹的判决序列 network ≡ numpy，**差异 0** |
| **R3 对 v2 封存一致性（numpy）** | **PASS** | **810 步**逐字段比对：判决类逐位一致、数值类 0 超容差、臂级汇总 0 超容差、计数 0 差异 |
| **G2 基线符合（oracle）** | **PASS** | 24 环境 + 24 训练批 + **250** 认证批哈希 + **576** 项首访核对 + 24 组 `v0/v*` 和，**0 失败**——覆盖**全部 24 环境、全部 12 步**（含生产未到达的步，检查不随早停缩水） |

**封存对照值逐一命中**：闭合 `A 17.91% / B 54.89% / C 74.39%`（v2 封存 `17.9% / 54.9% / 74.4%`）；
首步发出 `13 / 29 / 46`（封存同值）；配对 `C>A 46/48`、`C<A 0`（封存同值）；救出 `33`（封存同值）。

**有限主张**：在任务单冻结的协议下，用**不共享** `model.py`、MDP/训练生成器、数组生产者与判决实现的
自足实现，复现了"按状态保守更新缓解早停"的限定结论。**0 违规只是失败检测，不是概率证明；
本路线结果在对方路线封存并交叉验证之前一律为初步结果。**

---

## 2. 运行记录

| 项 | 值 |
|---|---|
| 精确命令 | `python docs/research_branches/FP-INDEP-001/claude/evaluate.py --output-dir results/FP-INDEP-001/claude/formal` |
| 环境（统计单元） | 24 个（mixing {0.08, 0.5} × task_index 12..23），各含 2 算法 × 3 臂 × 2 生产者 = **12 条轨迹**，共 **288** 条 |
| 唯一认证批 | **250**（1,048,576,000 项）；v2 用逐 (路线,记录,步) 重抽，故其 `items_drawn_total=2,088,763,392` 为重复抽取口径；本件按**唯一批**计，且内容哈希在 §6.1 有可比锚点 |
| 网络前向 | **810**（轨迹提前停止，故低于上限 3,456） |
| 总墙钟 | **1288.5 s（21.5 min）** |
| 退出码 | `0`，**无任何异常触发**（故 §6 高精度复核未被触发；仪器要求已在任务单 §6 自足化） |
| 封存往返完整性 | **176 个判决关键字段，0 不一致**（`seal_integrity.json`） |
| 环境 | Python 3.13.9 / numpy 2.4.6；CPU |

**失败与中断记录**：无中断、无异常、无重跑。烟测（`--limit 1`）输出留在 `_smoke/`，**明确非正式协议**。

## 3. 逐项明细

### 3.1 R1（分格验收，每格 48 条 `(environment, algorithm)` 记录）

| 格 | mean 闭合 A | mean 闭合 B | mean 闭合 C | 闭合差 C−A | 首步 A/B/C | win/loss | 救出 |
|---|---|---|---|---|---|---|---|
| **numpy** | 0.1791 | 0.5489 | 0.7439 | **0.5648** | 13/29/46 | **46/0** | **33/35** |
| **network** | 0.1791 | 0.5489 | 0.7439 | **0.5648** | 13/29/46 | **46/0** | **33/35** |

两格的数值在报告精度内**相同**——因为 R2 的判决逐位一致意味着两生产者的策略轨迹相同，
价值只随策略走；数值层面的差异在 `Q̂` 上（见 §3.3 的注记）。

**预登记带逐条对照**（每格）：闭合差 `≥0.40` ✅（0.5648）；`C 首步 ≥40` ✅（46）；`A 首步 ≤20` ✅（13）；
`C ≥ 2×A` ✅（46 ≥ 26）；`win ≥40` ✅（46）；`loss ≤2` ✅（0）；`救出 ≥25` ✅（33）；三臂零违规零退化 ✅。

### 3.2 R3（numpy 对 v2 封存包）

810 个格-步（停止格无后续步），全部字段比对：

- 判决类（`emitted`、`eta_selected`、`states_updated`、`ordered_reasons`、`min_pair_count_observed`）：**0 差异**；
- 数值类（`e_q`、`min_lb`、实测 sup 误差）：**0 超容差**（容差 `1e-9`）；
- 臂级汇总（`initial_v_sum`、`final_v_sum`、`initial_suboptimality`、`fraction_gap_closed`、`emitted_steps`）：**0 超容差**（`1e-6`）与**0 计数差异**；
- `oracle_audit.certificate_violation`：**全 false**。

### 3.3 R2（判决对应）

288 条轨迹（24 环境 × 2 算法 × 3 臂 × 两生产者配对）：**network 与 numpy 的判决序列逐位一致，0 差异**。

### 3.4 停发分布（仅描述）

288 条轨迹中 214 条在某步停发；停点分布 `{1:112, 2:6, 3:12, 4:12, 5:26, 6:8, 7:14, 8:2, 9:8, 11:8, 12:6}`。
**仅描述**；不主张收敛速率，不主张后段收益为零或非零。

## 4. 一个必须如实说明的数值现象（不引申）

最小价值增量 `+6.074e-10`（发出步中逐状态最小增量的最小值）——正，但在 float64 噪声量级。
**按上一轮审阅的教训，我不把它解释成"已经收敛/没有可改进的量"**：它没有触发 §6 的暂停
（该点安全余量正常），逐分量退化审计全部通过。它只说明：在那些步上，被认证的改进在数值上很小。
是否值得高精度复核，由任务单 §6 的判据决定——本次**未触发**（无一格 `|margin| ≤ 1e-10`）。

## 5. 结构性说明（必须随结论携带）

- 本路线的自足实现**仍共享 numpy 平台原语**（`default_rng`/`choice`/`random`/`dirichlet`/`integers`）——
  任务单 §3 明确把这视同 IEEE float64。**概念性错误风险只剩规格层**（已由四轮预审覆盖）与平台层（未审）。
- 基线模块**只作参考 oracle**（G2 与 R3 的比对），不进生产路径。
- **本路线的"独立"是相对基线代码而言**；相对 **GPT 路线**的独立性要等对方封存后由交叉验证判定——
  那一步（G1 两路线输入一致、P2/P3 数值一致、X1 结论一致）**尚未发生**。
- 本任务的最终状态要求**双方均为 PASS**；当前只有我这一侧。

## 6. 产物位置与哈希

| 产物 | 路径 | SHA256（前 16 位） |
|---|---|---|
| 正式结果（权威件） | `results/FP-INDEP-001/claude/formal/task_results.json` | `66ef448f7036db33` |
| 分析 | `results/FP-INDEP-001/claude/formal/analysis.json` | `6f165a62d040dc1a` |
| 输入清单 | `results/FP-INDEP-001/claude/formal/input_manifest.json` | `972ddf25ee4a506c` |
| G2 oracle 校验 | `results/FP-INDEP-001/claude/verification/g2.json` | `08ee47e7937e6cb1` |
| 运行日志 | `results/FP-INDEP-001/claude/formal_run.log` | `bb10696fe712260a` |
| 封存完整性 | `results/FP-INDEP-001/claude/formal/seal_integrity.json` | `facb6a8d520a7c02` |
| 理论门禁 | `docs/research_branches/FP-INDEP-001/claude/premise_audit.md` | — |
| 程序 | `docs/research_branches/FP-INDEP-001/claude/{evaluate,analyze,verify}.py` | — |
| smoke（非正式） | `results/FP-INDEP-001/claude/_smoke/` | — |

`results/` 被 Git 忽略，数据不进仓库；代码、门禁与本报告按任务单 §5 提交。

## 7. 后续

**等 GPT 路线封存 `results/FP-INDEP-001/codex/formal/` 后**，我写 `verification_of_other.md`：
G1（两路线输入哈希一致）、P2（判决逐位一致）、P3（数值容差）、X1（结论表一致）。
在那之前不读取对方任何内容。
